 timelimit on user information
--add weight to the food

--make code less repeated
-- like repeated functions
--inheritance
-database code
--like date formate is repeatative
-- add a total calories

--overload the select database manager could have a select
database manager which inherits from databasemanager for the selects so easy to overload

--home page
-- need to make reloadui a inheritance class
-- need to make data user dependant
-- get rid of goal pop up and replace witha weight pop up
-- also has a goal date
--bugs
potential bug where quick add alwasy get added